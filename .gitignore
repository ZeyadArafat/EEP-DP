# --- Build outputs (CMake and common build dirs) ---
/build/
/build-*/
/cmake-build-*/
/out/
/bin/
/lib/
/dist/
/target/

# If you do in-source builds (not recommended), keep this:
CMakeFiles/
CMakeCache.txt
cmake_install.cmake
install_manifest.txt
compile_commands.json
CTestTestfile.cmake
Testing/
CMakeScripts/

# --- Compiled objects / libs / executables ---
*.o
*.obj
*.lo
*.slo
*.la
*.a
*.lib
*.so
*.so.*
*.dylib
*.dll
*.exe
*.out
*.app

# --- Precompiled headers / modules / dependency files ---
*.gch
*.pch
*.d
*.dep
*.mod
*.smod

# --- Debug/Profiling/Instrumentation outputs ---
*.dSYM/
*.su
*.gcno
*.gcda
*.gcov
*.profraw
*.profdata
gmon.out
vgcore.*
callgrind.out.*
cachegrind.out.*
perf.data
perf.data.old

# --- Sanitizers, crash dumps, core files ---
core
core.*
*.core

# --- Logs and runtime artifacts ---
*.log
*.tmp
*.temp
*.swp
*.swo

# --- Editors / IDEs ---
# CLion / JetBrains
.idea/
*.iml
cmake-build-debug/
cmake-build-release/
cmake-build-relwithdebinfo/
cmake-build-minsizerel/

# Visual Studio Code
.vscode/
*.code-workspace

# --- OS / filesystem noise ---
# Linux / GNOME
.nfs*
.Trash-*
.fuse_hidden*
.directory

# --- Packaging / archives (usually not tracked) ---
*.zip
*.tar
*.tar.gz
*.tgz
*.7z
*.rar

# --- Python/other tooling caches (if you use scripts) ---
__pycache__/
*.pyc

# --- Local environment / secrets (keep templates instead) ---
.env
.env.*
!.env.example
*.local

# --- Optional: keep third-party deps if vendored, ignore if generated ---
# /third_party/        # Uncomment ONLY if generated by a package manager

# --- Optional: ccache (if stored in project) ---
.ccache/

# --- Windows OS noise ---
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
Desktop.ini
$RECYCLE.BIN/
*.lnk

# --- Visual Studio / MSBuild (if anyone uses it) ---
.vs/
*.VC.db
*.VC.VC.opendb
ipch/
*.sdf
*.opensdf
*.user
*.userosscache
*.suo
*.pdb
*.idb
*.ilk
*.tlog
*.lastbuildstate

# Visual Studio build folders
Debug/
Release/
x64/
x86/
Win32/
ARM/
ARM64/

# CMake on Windows can also generate these in-source sometimes
CMakeSettings.json

# --- MinGW / MSYS2 runtime artifacts (optional) ---
*.stackdump